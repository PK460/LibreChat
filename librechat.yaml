version: 1.3.11

mcpServers:
  ado-server:
    command: node
    args:
      - "/ado-mcp-server/server.js"
    startup: false
    timeout: 120000
    env:
      ADO_PAT: ${ADO_PAT}
      ADO_ORG: ${ADO_ORG}
      ADO_PROJECT: ${ADO_PROJECT}
